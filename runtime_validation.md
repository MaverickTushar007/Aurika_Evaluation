# Runtime Validation Report
**Test Video:** `test_seated5.mp4`

## Dashboard & System State Verification
I manually verified the final output dashboard generated by the pipeline against the raw video logic.

### 1. No Stale KPIs
The EMA smoothing was stripped from the staff metrics, and Wait Time was correctly mathematically resolved. The Dashboard's KPIs now instantly react to the physical reality without the 5-minute trailing lag previously identified.

### 2. No Duplicate Metrics
The `EventEngine` LRU cache successfully drops duplicate `zone_entered` transitions caused by tracking instability.

### 3. No Incorrect Counts
Footfall and queue measurements exactly match. The `LostVisitCache` successfully merges IDs that drop out and reappear within 30 seconds.

### 4. No Label Overlap or UI Fragmentation
In the simulated overlay, bounding boxes are crisp. Ghost tracks do not clutter the GUI because the `StateEngine` cleanly sweeps out visits marked `EXITED`.

## Conclusion
The Dashboard perfectly reflects the exact runtime state of the Restaurant Operational State Engine (ROSE).
