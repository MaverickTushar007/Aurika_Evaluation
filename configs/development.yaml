# Aurika AIP Development Profile
environment: development
log_level: DEBUG
storage:
  type: sqlite
  path: "./aip_dev.db"
cache:
  type: redis
  host: "localhost"
  port: 6379
security:
  jwt_secret: "dev-secret-key-change-in-prod"
  enable_cors: true
  rate_limit_per_min: 1000
